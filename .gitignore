# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
chapters/*.aux

# Editor
*.swp
*~
.DS_Store

# Build outputs (preserved by default; uncomment to ignore)
# *.pdf
# *.epub

# Personal brainstorming scratchpad (held out of the public repo; add -f to include)
notes.txt

# Browser-automation session artifacts (Playwright snapshots + KDP screenshots)
.playwright-mcp/
/kdp-*.png

# KDP local config (plugin-local working notes; the cover assets in kdp/ are tracked)
.claude/

# tex4ebook working directory (intermediate XHTML/SVG staging for `make epub`)
on-intelligence-epub3/
